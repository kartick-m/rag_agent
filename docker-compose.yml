version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: rag-agent
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./:/app
    command: gunicorn app:app -b 0.0.0.0:8080
